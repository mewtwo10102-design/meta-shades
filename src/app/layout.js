export const metadata = {
  title: 'Meta Shades ($SPECS) - See The Future',
  description: 'Turn any meme into legendary shades! The smoothest memecoin on Solana.',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
